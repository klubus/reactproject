import { FC } from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  color: black;
`;

const Entities: FC = () => {
  return <Wrapper>ENTITIES</Wrapper>;
};

export default Entities;
