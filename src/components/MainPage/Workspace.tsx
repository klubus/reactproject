import { FC } from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  color: black;
`;

const Workspace: FC = () => {
  return <Wrapper>Workspace</Wrapper>;
};

export default Workspace;
